using System;

namespace YTDlpSharp.Settings
{
    /// <summary>
    /// Настройки планировщика (заготовка для будущей версии)
    /// </summary>
    public class SchedulerSettings
    {
        // Этот класс будет реализован в будущих версиях
        // Пока оставляем пустым или с минимальной структурой

        /// <summary>
        /// Использовать планировщик задач
        /// </summary>
        public bool UseScheduler { get; set; } = false;

        /// <summary>
        /// Конструктор по умолчанию
        /// </summary>
        public SchedulerSettings() { }
    }
}
